/**
 * 내부 아이콘 래퍼 컴포넌트
 * 자동 생성된 아이콘 파일에서 공통 props(size, color, style) 적용에 사용
 * 사용자는 이 컴포넌트 대신 Icon, BrandIcon, FlagIcon 등을 사용해야 함
 */
import { type ReactNode } from 'react';
import type { Props } from './IconWrapper.types';
export declare const Icon: import("react").ForwardRefExoticComponent<Omit<Props & {
    children: ReactNode;
}, "ref"> & import("react").RefAttributes<SVGSVGElement>>;
